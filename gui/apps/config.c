// gui/apps/config.c — placeholder para configurações (abre about por enquanto)
#include "config.h"
#include "about.h"

void open_config_window(void) {
    // Por enquanto, apenas abre a janela Sobre
    open_about_window();
}